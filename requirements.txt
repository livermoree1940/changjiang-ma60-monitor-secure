pandas
matplotlib
adata          # 用于获取股票行情数据（确保安装最新版本）
# 邮件发送相关（utils_email 内可能依赖 smtplib 或第三方库）
yagmail       # 可选，如果 utils_email 使用 yagmail
email-validator  # 可选

# 交易日判断
exchange_calendars  # 如果 is_trade_day() 依赖
