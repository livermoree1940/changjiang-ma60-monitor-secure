pandas>=1.5.0
matplotlib>=3.7.0
adata>=0.1.0
numpy>=1.24.0
yagmail>=0.15.0          # 如果 utils_email 依赖 yagmail 发送邮件
email-validator>=1.3     # 可选，用于邮件验证
exchange-calendars>=2.5  # 替代原先不可用的 trade-calendar
